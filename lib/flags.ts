// Feature flags.
//
// Multi-language translation (UI chrome + AI-content localization). Turned OFF —
// the machine translations were low-value, so everything renders in English.
// Flip to true to bring it back (the messages/*.json + generator all still ship).
export const I18N_ENABLED = false;
